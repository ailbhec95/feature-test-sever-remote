import { FlagConfig } from '../types/flag';
import { HttpClient } from '../types/transport';
export declare class FlagConfigFetcher {
    private readonly logger;
    private readonly apiKey;
    private readonly serverUrl;
    private readonly flagApi;
    private receiver;
    constructor(apiKey: string, httpClient: HttpClient, serverUrl?: string, debug?: boolean);
    /**
     * Fetch local evaluation mode flag configs from the Experiment API server.
     * These flag configs can be used to perform local evaluation.
     *
     * @returns The local evaluation mode flag configs for the initialized
     * environment
     */
    fetch(): Promise<Record<string, FlagConfig>>;
    setRawReceiver(rawReceiver: (flags: string) => void): void;
}
