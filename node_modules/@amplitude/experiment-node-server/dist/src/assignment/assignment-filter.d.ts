import { Assignment, AssignmentFilter } from './assignment';
export declare class InMemoryAssignmentFilter implements AssignmentFilter {
    private readonly cache;
    constructor(size: number, ttlMillis?: number);
    shouldTrack(assignment: Assignment): boolean;
}
