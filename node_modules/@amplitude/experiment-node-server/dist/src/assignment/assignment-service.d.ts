import { BaseEvent } from '@amplitude/analytics-types';
import { CoreClient } from '@amplitude/analytics-types';
import { Assignment, AssignmentFilter, AssignmentService } from './assignment';
export declare const DAY_MILLIS: number;
export declare const FLAG_TYPE_MUTUAL_EXCLUSION_GROUP = "mutual-exclusion-group";
export declare class AmplitudeAssignmentService implements AssignmentService {
    private readonly amplitude;
    private readonly assignmentFilter;
    constructor(amplitude: CoreClient, assignmentFilter: AssignmentFilter);
    track(assignment: Assignment): Promise<void>;
}
export declare const toEvent: (assignment: Assignment) => BaseEvent;
